;; This is a comment
struct Pos{
    x: Int,
    y: Int,
	foo: fn() {
		print("This is a method")
	}
}

fn main(){
    print("Hello World")
    let mut pos_player = Pos(10, 15)
	pos_player.x = if pos_player.y > 12 {pos_player.x + 5} else {pos_player.x - 5}
}

fn pure double(x: Int) -> Int: x*2
fn pure double(x: Int) -> Int  x*2
fn pure double(x: Int): x*2
fn pure double(x: Int)  x*2
fn pure double(x: Int) -> Int {
	x*2
}


